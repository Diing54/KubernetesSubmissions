apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-claim
  namespace: project
spec:
  storageClassName: todo-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
